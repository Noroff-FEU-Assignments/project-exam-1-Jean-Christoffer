const selectors = ['.arrow-right','.arrow-left','.slider']
const selected = selectors.forEach((element) => document.querySelector(element))
const [arrowRight, arrowLeft, slider] = selected

function carousel(){
    
}


export default carousel